/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor
if image_index = 8
{
	instance_destroy()
}

var offset = 16;

x = obj_player.x + lengthdir_x(offset, dir);
y = obj_player.y + lengthdir_y(offset, dir);